spring.application.name=demo
server.port=${PORT:8080}

spring.datasource.url=jdbc:mysql://localhost:3306/momentum?connectionTimeZone=UTC&preserveInstants=true
spring.datasource.username=momentum
spring.datasource.password=${MYSQL_PASSWORD}
spring.sql.init.mode=always

momentum.parser.model=claude-haiku-4-5-20251001
momentum.parser.enabled=true
momentum.parser.max-input-length=280